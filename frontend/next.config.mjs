/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'raw.githubusercontent.com',
            'images.unsplash.com',
            'upload.wikimedia.org',
            'www.naturalcore.co',
        ]
    }
};

export default nextConfig;
